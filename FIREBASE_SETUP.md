# 🔥 Firebase 設定指南

本遊戲使用 Firebase Realtime Database 來實現多裝置即時同步功能。

## 📝 設定步驟

### 1️⃣ 建立 Firebase 專案

1. 前往 [Firebase Console](https://console.firebase.google.com/)
2. 點擊「新增專案」或「Add project」
3. 輸入專案名稱（例如：`cny-bingo`）
4. 不需要 Google Analytics（可以關閉）
5. 點擊「建立專案」

### 2️⃣ 建立 Realtime Database

1. 在左側選單選擇「Build」→「Realtime Database」
2. 點擊「建立資料庫」或「Create Database」
3. 選擇地區（建議選擇 `asia-southeast1` 新加坡較近）
4. **安全性規則選擇「測試模式」**（Test mode）
   - 這會開放讀寫權限，方便測試
   - 注意：測試模式有效期限 30 天

### 3️⃣ 取得 Firebase 配置

1. 點擊專案設定（齒輪圖示）→「專案設定」
2. 往下滾動到「你的應用程式」區域
3. 點擊「Web」圖示（`</>`）
4. 輸入應用程式暱稱（例如：`cny-bingo-web`）
5. **不需要**勾選 Firebase Hosting
6. 點擊「註冊應用程式」

你會看到類似這樣的配置：

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "your-project.firebaseapp.com",
  databaseURL: "https://your-project-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef1234567890"
};
```

### 4️⃣ 更新遊戲配置

1. 打開 `index.html`
2. 找到檔案開頭的 Firebase 配置區域（約在第 390 行附近）
3. 將你的 Firebase 配置貼上替換範例配置

```javascript
// 🔥 Firebase 配置 - 請替換成你自己的配置
const firebaseConfig = {
  apiKey: "你的-apiKey",
  authDomain: "你的-authDomain",
  databaseURL: "你的-databaseURL",  // ⚠️ 這個很重要！
  projectId: "你的-projectId",
  storageBucket: "你的-storageBucket",
  messagingSenderId: "你的-messagingSenderId",
  appId: "你的-appId"
};
```

### 5️⃣ 設定資料庫安全性規則（建議）

測試完成後，建議更新安全性規則：

1. 回到 Realtime Database 頁面
2. 選擇「規則」分頁
3. 使用以下規則（允許所有人讀寫房間，但有時間限制）：

```json
{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        ".indexOn": ["createdAt"]
      }
    }
  }
}
```

### 6️⃣ 提交並推送

```bash
git add index.html
git commit -m "設定 Firebase 配置"
git push
```

等待 GitHub Pages 重新部署（約 1-3 分鐘），就可以測試多裝置同步功能了！

## 🎮 如何使用

1. **建立房間**
   - 第一個人打開遊戲
   - 輸入玩家名字
   - 點擊「建立房間」
   - 會產生 6 位數房間碼（例如：`ABC123`）

2. **加入房間**
   - 其他人打開遊戲
   - 選擇「加入房間」
   - 輸入房間碼
   - 輸入自己的名字

3. **開始遊戲**
   - 主持人（建立房間的人）點「開獎」
   - 所有人的裝置會自動同步標記
   - 先連線的人會看到賓果動畫

## 🔒 隱私與安全

- 房間碼是隨機生成的 6 位英數字
- 資料只保存在遊戲進行期間
- 不會收集個人資訊
- 建議遊戲結束後刪除房間（Firebase Console 可手動刪除）

## ❓ 常見問題

**Q: Firebase 要錢嗎？**
A: 免費方案足夠使用。每月免費額度：
- 同時連線數：100
- 儲存空間：1 GB
- 下載流量：10 GB

一般家庭過年使用絕對夠用！

**Q: 測試模式安全嗎？**
A: 測試模式會在 30 天後自動關閉寫入權限。建議：
- 遊戲結束後到 Firebase Console 刪除資料
- 或使用上面建議的安全性規則

**Q: 可以不設定 Firebase 嗎？**
A: 可以！如果不設定 Firebase：
- 遊戲會自動降級為單機模式
- 功能與原版相同（共享螢幕模式）
- 不支援多裝置同步

## 🆘 需要協助？

如有問題請到 [GitHub Issues](https://github.com/jameslu8/cny-bingo/issues) 回報。

---

🧧 **設定完成，恭喜發財！** 🧧
